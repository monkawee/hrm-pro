1. สร้าง Environment: python -m venv .venv
2. เปิดใช้งาน: * (Windows): .venv\Scripts\activate
    (Mac/Linux): source .venv/bin/activate
3. ลง Library ที่จำเป็น: pip install fastapi uvicorn jinja2 python-multipart